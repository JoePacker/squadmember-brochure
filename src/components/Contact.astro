<section id="contact" class="contact">
	<div class="wrap">
		<div class="intro">
			<div class="eyebrow">Get in touch</div>
			<h2>Let's talk about your club</h2>
			<p>
				Tell us a bit about your club and what you need &mdash; we'll come back with all the steps forward to your new site.
			</p>
			<div class="info">
				<div>
					<div class="info-label">Email</div>
					<a href="mailto:hello@squadmember.com">hello@squadmember.com</a>
				</div>
				<div>
					<div class="info-label">Based</div>
					<div class="info-value">Swansea, Wales</div>
				</div>
			</div>
		</div>

		<div class="form-card">
			<form id="contact-form" action="https://api.web3forms.com/submit" method="POST">
				<input type="hidden" name="access_key" value="d74127ea-a7c9-430c-bf5d-4853d8592cd2" />
				<input type="hidden" name="redirect" value="https://squadmember.com/success" />
				<div class="row">
					<div class="field">
						<label for="name">Name</label>
						<input id="name" name="name" type="text" placeholder="Your name" required />
					</div>
					<div class="field">
						<label for="club">Club name</label>
						<input id="club" name="club" type="text" placeholder="e.g. Squadmember RFC" required />
					</div>
				</div>
				<div class="field">
					<label for="email">Email</label>
					<input id="email" name="email" type="email" placeholder="you@club.co.uk" required />
				</div>
				<div class="field">
					<label for="message">What do you need?</label>
					<textarea
						id="message"
						name="message"
						rows="4"
						placeholder="Tell us about your club and what you're after"
						required></textarea>
				</div>
				<button type="submit" class="submit">Send message</button>
			</form>
		</div>
	</div>
</section>

<style>
	.contact {
		background: var(--forest);
		padding: 120px 48px;
	}

	.wrap {
		max-width: 1100px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 0.9fr 1.1fr;
		gap: 80px;
	}

	.eyebrow {
		color: var(--green-light);
		font-size: 13px;
		font-weight: 700;
		letter-spacing: 3px;
		text-transform: uppercase;
		margin-bottom: 14px;
	}

	h2 {
		font-family: 'Bebas Neue', sans-serif;
		font-size: 48px;
		letter-spacing: 0.5px;
		margin: 0 0 20px;
		color: var(--cream);
	}

	.intro p {
		color: var(--cream-muted);
		font-size: 16px;
		line-height: 1.65;
		margin: 0 0 36px;
	}

	.info {
		display: flex;
		flex-direction: column;
		gap: 18px;
	}

	.info-label {
		color: var(--muted);
		font-size: 12px;
		text-transform: uppercase;
		letter-spacing: 1px;
		margin-bottom: 4px;
	}

	.info a,
	.info-value {
		color: var(--cream);
		font-size: 16px;
		font-weight: 600;
	}

	.form-card {
		background: var(--forest-light);
		border-radius: 8px;
		padding: 40px;
		border: 1px solid rgba(244, 239, 227, 0.08);
	}

	form {
		display: flex;
		flex-direction: column;
		gap: 22px;
	}

	.row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 22px;
	}

	label {
		display: block;
		color: var(--cream-muted);
		font-size: 13px;
		margin-bottom: 8px;
	}

	input,
	textarea {
		width: 100%;
		box-sizing: border-box;
		background: transparent;
		border: none;
		border-bottom: 1.5px solid rgba(244, 239, 227, 0.25);
		color: var(--cream);
		font-size: 15px;
		padding: 8px 0;
		outline: none;
		font-family: inherit;
	}

	input:focus,
	textarea:focus {
		border-bottom-color: var(--green-light);
	}

	textarea {
		resize: vertical;
	}

	.submit {
		align-self: flex-start;
		background: var(--green-light);
		color: var(--forest);
		border: none;
		padding: 15px 34px;
		border-radius: 999px;
		font-weight: 800;
		font-size: 14px;
		text-transform: uppercase;
		letter-spacing: 0.3px;
		cursor: pointer;
	}

	.submit:hover {
		background: var(--green-lighter);
	}

	@media (max-width: 900px) {
		.contact {
			padding: 80px 24px;
		}

		.wrap {
			grid-template-columns: 1fr;
			gap: 48px;
		}

		.row {
			grid-template-columns: 1fr;
		}

		.form-card {
			padding: 28px;
		}
	}
</style>
