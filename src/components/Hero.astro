<section class="hero">
	<div class="hero-lines" aria-hidden="true"></div>
	<div class="hero-glow" aria-hidden="true"></div>
	<div class="hero-content">
		<div class="eyebrow">Grassroots rugby websites &middot; Wales</div>
		<h1>Websites for the clubs<br />that turn up every week.</h1>
		<p>
			At squadmember we build fast, easy-to-manage websites for grassroots rugby clubs across
			Wales &mdash; fixtures, teams, sponsors and membership, all sorted.
		</p>
		<div class="hero-actions">
			<a href="#contact" class="btn-primary">Get a quote</a>
		</div>
		<div class="hero-footnote">
			From from mini &amp; juniors right through to the first XV
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		background: var(--forest);
		padding: 110px 48px 130px;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		overflow: hidden;
	}

	.hero-lines {
		position: absolute;
		inset: 0;
		background: repeating-linear-gradient(
			115deg,
			rgba(244, 239, 227, 0.035) 0px,
			rgba(244, 239, 227, 0.035) 2px,
			transparent 2px,
			transparent 60px
		);
		pointer-events: none;
	}

	.hero-glow {
		position: absolute;
		top: -180px;
		right: -160px;
		width: 520px;
		height: 520px;
		border-radius: 50%;
		background: radial-gradient(circle, rgba(200, 118, 63, 0.18), transparent 70%);
		pointer-events: none;
	}

	.hero-content {
		position: relative;
		max-width: 900px;
	}

	.eyebrow {
		color: var(--green-light);
		font-size: 14px;
		font-weight: 700;
		letter-spacing: 3px;
		text-transform: uppercase;
		margin-bottom: 22px;
	}

	h1 {
		font-family: 'Bebas Neue', sans-serif;
		color: var(--cream);
		font-size: 86px;
		line-height: 0.98;
		letter-spacing: 0.5px;
		margin: 0 0 26px;
	}

	.hero-content p {
		color: var(--cream-muted);
		font-size: 19px;
		line-height: 1.6;
		max-width: 620px;
		margin: 0 auto 40px;
	}

	.hero-actions {
		display: flex;
		gap: 16px;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn-primary,
	.btn-secondary {
		padding: 16px 32px;
		border-radius: 999px;
		font-size: 15px;
		font-weight: 800;
		letter-spacing: 0.3px;
		text-transform: uppercase;
	}

	.btn-primary {
		background: var(--green-light);
		color: var(--forest);
	}

	.btn-primary:hover {
		background: var(--green-lighter);
	}

	.btn-secondary {
		border: 1.5px solid rgba(244, 239, 227, 0.4);
		color: var(--cream);
	}

	.btn-secondary:hover {
		border-color: var(--cream);
		color: var(--cream);
	}

	.hero-footnote {
		margin-top: 56px;
		padding-top: 28px;
		border-top: 1px dashed rgba(244, 239, 227, 0.2);
		color: var(--muted);
		font-size: 13.5px;
		letter-spacing: 0.5px;
	}

	@media (max-width: 720px) {
		.hero {
			padding: 80px 24px 90px;
		}

		h1 {
			font-size: 48px;
		}

		.hero-content p {
			font-size: 17px;
		}
	}
</style>
